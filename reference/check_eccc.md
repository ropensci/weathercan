# Check access to ECCC

Checks if whether there is internet access, weather data, normals data,
and eccc sites are available and accessible, and whether we're NOT
running on cran

## Usage

``` r
check_eccc()
```

## Value

FALSE if not, TRUE if so

## Examples

``` r
check_eccc()
#> [1] FALSE
```
