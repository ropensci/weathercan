# Remove cached directory and contents

This is a helper function to remove weathercan's cache directory. This
folder contains updated stations inventory lists and the full dataset of
Climate normals for 1991-2010.

## Usage

``` r
cache_remove()
```

## Value

nothing but removes directory

## Examples

``` r
if (FALSE) { # interactive()
cache_remove()
}
```
